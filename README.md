#GITHUB

git init
git config --global user.email "sandromesquita@yahoo.com"
git config --global user.name "Sandro Mesquita"
git add .
git commit -m "COMMIT inicial"
git remote add origin https://github.com/ssmesquita/primeiro-spring-app.git
git push -u origin main
git branch -M main

